services:
  immichmcp:
    # Fijado al digest en uso el 2026-08-07 (era :latest). Para actualizar:
    # docker pull ghcr.io/barryw/immichmcp:latest && docker inspect --format
    # '{{index .RepoDigests 0}}' ghcr.io/barryw/immichmcp:latest
    image: ghcr.io/barryw/immichmcp@sha256:3f3148232c43567df42501957778dde421abeb59cfa9f622ee58af5d39da8577
    container_name: immichmcp
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:5000:5000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
