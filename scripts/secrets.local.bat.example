@echo off
rem Copia este archivo como "secrets.local.bat" (sin ".example") en la misma
rem carpeta y rellena tus valores reales. Ese archivo copiado NUNCA se sube
rem al repositorio (ver .gitignore).
set PAPERLESS_TOKEN=TU_TOKEN_AQUI
rem Clave de solo lectura de Immich (Ajustes -> API Keys). La usa
rem listar_personas en mcp_fotos.py, vía GET /api/people y
rem POST /api/search/metadata: basta con person.read, asset.read y face.read.
set IMMICH_API_KEY=TU_CLAVE_DE_IMMICH_AQUI
